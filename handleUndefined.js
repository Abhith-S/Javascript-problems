//The double function here is almost complete! We also want double to be able to handle when x is undefined.
//If no value is provided for x, return 0.
//else return double of x

function double(x) {
    return (x || 0) * 2;
    
}

double()
